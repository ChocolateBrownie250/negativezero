{
  "name": "@negativezero/admin",
  "private": true,
  "version": "1.0.0",
  "description": "Platform-level admin: passkey-protected registration-code generator for negativezero services",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w client run build && npm -w server run build",
    "start": "npm -w server run start",
    "test": "npm -w server run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
